version: 1

project:
  name: "maru-web"

mode: shadow

test:
  detected: false

risk:
  historical_memory: true

llm:
  provider: auto

security:
  redact_secrets: true

contracts:
  directory: .maru/contracts

artifacts:
  directory: .maru/artifacts
