# MaruCheck web foundation

The root page must identify the product as MaruCheck and explain that it independently verifies AI-generated software.

The health endpoint must respond to GET requests with a successful JSON status without exposing configuration or secrets.

The web application must remain independently buildable and deployable from the MaruCheck CLI repository.
