# Optional project-scoped MaruCheck MCP integration for Codex.
# Pinning the public package keeps this portable when the CLI repository is not present.
[mcp_servers.maru]
command = "npx"
args = ["--yes", "marucheck@0.2.2", "mcp"]
required = false
default_tools_approval_mode = "writes"
